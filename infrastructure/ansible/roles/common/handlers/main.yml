---
- name: Restart chrony
  ansible.builtin.systemd_service:
    name: "{{ common_ntp_service }}"
    state: restarted
  become: true

- name: Restart journald
  ansible.builtin.systemd_service:
    name: systemd-journald
    state: restarted
  become: true
