# F-17 — "the key is pinned, the control is not"

F-17 is `PARTIAL`, and the row says exactly why in one sentence:

> Invitation throttle keyed on an unvalidated header. The **key** is soundly
> pinned and the defect does not reproduce. The **control is not**: removing
> `throttle:team-invitations` from both routes leaves the suite green, so the
> finding's outcome is reachable again by a one-line edit. Reopened to
> `PARTIAL` on that evidence; closes when the oracle covers attachment.

So this round is narrow and its shape is already decided. **Do not re-litigate
the key.** The limiter's own keying is sound and pinned; that half is done and
touching it is out of scope.

## What is actually missing

Traced read-only from the main checkout at `7ab4248`, and you should re-measure
all of it rather than take it:

- `routes/v1/team.php:37` and `:42` each carry
  `->middleware('throttle:team-invitations')`.
- `src/Providers/RateLimitServiceProvider.php:168` registers
  `RateLimiter::for('team-invitations', …)`, and `:147` carries the docblock
  about the `X-Lynomia-Customer` header that this finding is named after.
- Two tests already look at throttling across the route table:
  `tests/Feature/Security/EveryAuthenticatedRouteIsThrottledTest.php` and
  `tests/Feature/Security/OneThrottleBucketPerVerbTest.php`.

**The hole is the gap between "throttled" and "throttled by this limiter."**
The route group already applies `throttle:api`, so a route that loses
`throttle:team-invitations` is still throttled and still passes a test that
asks only whether a throttle is attached. That is why deleting the line leaves
the suite green.

## Your first move is the red, and it is one line

Delete `->middleware('throttle:team-invitations')` from **one** of the two
routes, run the suite, and confirm it stays green. Then delete it from both and
confirm the same. **Report both numbers.** If either goes red, the row's
evidence is stale and that is a result — say so in those words and stop to
report it rather than building a fix for a hole that has closed.

Restore by copying back the pre-image you took before the edit, verified by
sha256. Never `git checkout --`.

## What to build

An oracle that covers **attachment**, not merely throttling. Beyond that I am
deliberately not specifying the shape, because the two obvious shapes have
different costs and I want you to argue between them rather than take mine:

- **(a) A named-route assertion**: these two routes must carry this limiter.
  Cheap, exact, and it is a list — so it says nothing about the route added
  next to them tomorrow.
- **(b) A rule over the route table**: any route reaching the invitation
  controller (or: any route whose limiter is a *named* limiter registered in
  `RateLimitServiceProvider`) must carry the limiter registered for it. More
  expensive, and it is the shape that survives a third route.

Read `EveryAuthenticatedRouteIsThrottledTest` before choosing: if (b) is
already half-built there, extending it beats a new file, and if it is not, say
why. **Whichever you pick, say what it does NOT cover** — this programme's
standing complaint is a list presented as a rule.

A third possibility is open and you should consider it rather than assume it
away: that the durable fix is not a test at all but a **registration-time
assertion** — a limiter registered and attached to no route, or a route naming
a limiter that is not registered, could be made impossible rather than
detected. Argue for or against; do not silently skip it.

## Mutation discipline, four obligations

1. Prove the edit landed (sha256 before and after, plus a whole-file
   `diff -u0` showing exactly the lines you meant).
2. Prove it landed **where you meant** — anchor by string, never by line
   number.
3. Prove it is behaviour-preserving except for the property under test.
4. **Restore against the value taken before, not against HEAD.**

And: **a harness that refuses to mutate must also refuse to run.** Self-test it
with a non-existent anchor before you trust a verdict from it.

Your new oracle must die to at least these, and say which mutant kills which
assertion:

- the middleware removed from route one;
- removed from route two;
- removed from both;
- `throttle:team-invitations` replaced by `throttle:api` (the substitution a
  careless edit actually makes);
- the `RateLimiter::for('team-invitations', …)` registration deleted.

That last one is the interesting one: if your oracle reads the route table
only, a route naming a limiter nobody registered may sail through, and Laravel
resolves an unregistered named limiter at request time rather than boot time.
**Measure what actually happens** in that case rather than reasoning about it.

## Proof standard

Three proofs, all three required:

- a JSON summary with **no `skipped` key** and `tests == passed`;
- `--log-junit`, with **every** `testsuite` element at `skipped="0"`;
- `ARTISAN-TEST EXIT=0` — take it with `${PIPESTATUS[0]}`, not `$?` after a
  pipe.

Run `./vendor/bin/pint --test` **at tree scope from `apps/control-plane`**,
which is CI's `working-directory`. It is a first-class gate.

Test counts add across paths. **Assertion totals do not.**

## Standing constraints

- **No real infrastructure.** No providers, credentials, payments, registrars,
  no apply, no deploy, no Docker. Work against the simulators.
- Your worktree is `/home/user/worktrees/f17a`, branch `remediation/f17a`,
  database `lynomia_test_f17a`, Redis index **3**. Work only there.
- Never `git stash` — the stash stack is shared across worktrees.
- Never `composer install` or `composer dump-autoload`.
- Never edit `vendor/` in place; use
  `scratchpad/unshare-vendor-package.sh f17a <ns>/<pkg>`.
- Never `pkill` or `killall` anything you did not start.
- **Never run a bare `php artisan migrate:fresh`.** `.env` carries
  `APP_ENV=local` and `DB_DATABASE=lynomia`, so from `apps/control-plane` it
  lands on the **application** database. An agent in this programme has already
  done it.
- One `php artisan test` at a time against your database.
- A known flake: `TheBridgeRefusesToRunWhereItMustNotTest` fails about one full
  run in 2,300 for a reason that is not yours.

**Paths you may need, absolute, because a bare relative path from your worktree
resolves to `apps/control-plane/scratchpad/` and these are not there:**

- the ledger (read it, do not edit it; your worktree's copy is frozen, which is
  a defect of mine recorded in it):
  `/home/user/cloud/docs/round-2-remediation-ledger.md`
- the adjudication rule, which you must read before judging any sentence:
  `/home/user/cloud/docs/round-2-briefs/adjudication-rule.md`

You do **not** have authority to declare F-17 closed. Hand back; a separate
independent verifier decides.

## What I want back

The two deletion measurements first, whatever they say. Then the oracle, the
argument between (a), (b) and the registration-time option, the mutation table
with one row per mutant and which assertion died, the three proofs, and — in
those words — anything you **could not establish**.
