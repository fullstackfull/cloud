# F-44 brief — traced 2026-09-24, ready to dispatch

**Finding (audit, verbatim):** "`QuarantineLifecycleTest` carries the reboot-race clock shape in three methods,
missed by the prior closure sweep."

The audit states this as a **correction to the coordinator's own prior flake-closure work**: the earlier document
said seven other files using `now()` in fixtures were checked and none had the shape — *"true of those seven and
incomplete as a sweep"*. The shape: `now()->addDays(7)` read at assertion time against a value the application
wrote at fixture time, with `travel()` calls present but **no `freezeTime()`**. Day granularity, so it misfires
only across midnight UTC. The audit adds a quantifier: *"A targeted tree-wide sweep finds exactly two files with
the precise shape: the one that was fixed, and this one."*

## What I confirmed by reading

`apps/control-plane/tests/Feature/Ipam/QuarantineLifecycleTest.php`, 286 lines. The three assertion-time reads:

- `:65`  `now()->addDays(7)->toDateString()`
- `:87`  `now()->addDays(7 * IpPool::ABUSE_QUARANTINE_MULTIPLIER)->toDateString()`
- `:107` `now()->addDay()->toDateString()`

`travel()` appears at `:119 :127 :152 :180 :224 :254`; `freezeTime()` appears nowhere in the file.

## Scope notes for the brief

- **The "exactly two files" claim is a measurement, not a fact to inherit.** The round re-runs the sweep and
  reports its own number and its own definition of "the precise shape" — and must drift-test the sweep, because
  a sweep nobody has watched fail is not a sweep.
- Reproduce before fixing: the misfire is real only across midnight UTC, so the first-fail proof is a clock
  positioned to cross it, not a re-run and a shrug.
- **Do not touch production code.** This is TEST_GAP. If the fix appears to need a production change, that is a
  finding about the production code and belongs in the hand-back, not in the diff.
- F-34 is in the same area and is instructed not to touch this file.
